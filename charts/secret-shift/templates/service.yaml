apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-shift.fullname" . }}
  labels:
    {{- include "secret-shift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-shift.selectorLabels" . | nindent 4 }}
