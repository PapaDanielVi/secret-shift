apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secret-shift.fullname" . }}-test-connection"
  labels:
    {{- include "secret-shift.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "secret-shift.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
